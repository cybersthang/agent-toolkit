---
tasks_for: v0.13.0-clarification-gate-enforcer
status: approved
created: 2026-05-23
last_updated: 2026-05-23
auto_chain_after_approval: true
approved_at: 2026-05-23T18:14:07+07:00
layout_note: "Flat dot-naming (upstream agent-toolkit convention) — matches v0.7.1-implement-notes.verify_report.md sibling pattern. KHÔNG migrate sang <branch>/<slug>/tasks.md vì sẽ phá convention hiện có của repo này."
---

# Tasks — v0.13.0 clarification-gate-enforcer

Spec: [v0.13.0-clarification-gate-enforcer.md](v0.13.0-clarification-gate-enforcer.md) (status: clarified, eval_status: defined)

Total: **21 tasks** · **~440 LOC** budget · all tasks ≤ 30 LOC trừ T15 (~40 LOC, justified inline).

---

## T1 — Thêm regex `SKIP_CLARIFICATION_RE` vào `_patterns.py`

- **Touches:** `templates/claude/hooks/_patterns.py`
- **Depends on:** none
- **Acceptance:** Module import được constant `SKIP_CLARIFICATION_RE`; pattern là `re.compile(r'\bskip-clarification:\s*(\S{8,200})\b', re.IGNORECASE)`; reason ≥ 8 ký tự non-whitespace, ≤ 200 ký tự match được; `skip-clarification: x` (1 char) KHÔNG match. Covers D9.
- **Verification:** `python -c "from templates.claude.hooks._patterns import SKIP_CLARIFICATION_RE; assert SKIP_CLARIFICATION_RE.search('skip-clarification: emergency-fix'); assert not SKIP_CLARIFICATION_RE.search('skip-clarification: x')"` exits 0.
- **Risk:** low

## T2 — Thêm `LAST_INTENT_SUGGESTED_REL` constant + `_write_last_intent_suggested()` helper vào `intent_router.py`

- **Touches:** `templates/claude/hooks/intent_router.py`
- **Depends on:** none
- **Acceptance:** Hàm helper ghi `.agent-toolkit/.last_intent_suggested.json` với schema `{ts: <epoch>, skills: [...], prompt_hash: <sha256[:8]>}`. Atomic write (temp file + rename). Constant `LAST_INTENT_SUGGESTED_REL = ".agent-toolkit/.last_intent_suggested.json"` đặt cạnh `BYPASS_FILE_REL`.
- **Verification:** Unit test `python -c "from templates.claude.hooks.intent_router import _write_last_intent_suggested; ..."` tạo file đúng schema, đọc lại được bằng `json.loads()`.
- **Risk:** low

## T3 — Wire `_write_last_intent_suggested()` vào `main()` của `intent_router.py`

- **Touches:** `templates/claude/hooks/intent_router.py`
- **Depends on:** T2
- **Acceptance:** Khi `_matched_skills()` return list chứa `"clarification-gate"`, gọi `_write_last_intent_suggested(workspace, skills, prompt)` TRƯỚC `print(json.dumps(output))`. Không break behavior cũ (vẫn print system-reminder).
- **Verification:** Manual: chạy `echo '{"prompt":"fix this bug"}' | python intent_router.py` → file `.last_intent_suggested.json` xuất hiện + stdout vẫn có system-reminder.
- **Risk:** low

## T4 — Thêm `_capture_skip_clarification()` helper vào `intent_router.py`

- **Touches:** `templates/claude/hooks/intent_router.py`
- **Depends on:** T1
- **Acceptance:** Hàm parse prompt bằng `SKIP_CLARIFICATION_RE`; match → ghi `.agent-toolkit/.skip_clarification_next.json` `{ts, reason, ttl: 300}`. Pattern song song với `_capture_bypass_invariant()` đã có (line 402-441).
- **Verification:** Unit test: prompt `"skip-clarification: emergency-fix"` → file tạo + `reason == "emergency-fix"`. Prompt `"skip-clarification: x"` → no file.
- **Risk:** low

## T5 — Wire `_capture_skip_clarification()` vào `main()` TRƯỚC short-prompt early-out

- **Touches:** `templates/claude/hooks/intent_router.py`
- **Depends on:** T4
- **Acceptance:** Call point ở line ~462 (cạnh `_capture_bypass_invariant`), TRƯỚC `if len(prompt) < 12: return 0` — vì escape token có thể short nhưng load-bearing (giống bypass-invariant pattern).
- **Verification:** Test: prompt 30-char `"skip-clarification: hotfix-now"` → state file ghi. Prompt 200-char không match → no file.
- **Risk:** low

## T6 — Tạo `clarification_gate_enforcer.py` skeleton + recursion break

- **Touches:** `templates/claude/hooks/clarification_gate_enforcer.py` (new)
- **Depends on:** none
- **Acceptance:** Module mới copy structure từ `evidence_audit.py` (shebang, `wrap_utf8_stdio`, `run_main_safe`); `main()` parse envelope; nếu `envelope.get("stop_hook_active") == True` → exit allow ngay (recursion break). Constants: `LAST_INTENT_REL`, `SKIP_REL`, `AUTONOMY_REL`, `STATE_TTL = 600`.
- **Verification:** `python clarification_gate_enforcer.py < /dev/null` exits 0; `echo '{"stop_hook_active":true}' | python clarification_gate_enforcer.py` exits 0 silent.
- **Risk:** low

## T7 — Thêm skip paths: autonomy + no-suggestion + expired-state vào enforcer

- **Touches:** `templates/claude/hooks/clarification_gate_enforcer.py`
- **Depends on:** T6
- **Acceptance:** 3 skip paths theo thứ tự:
  1. `AUTONOMY_REL` tồn tại + chưa expired → exit allow + `log_fire(bypass=["autonomy"])`.
  2. `LAST_INTENT_REL` không tồn tại HOẶC `"clarification-gate"` not in skills → exit allow silent.
  3. State file > `STATE_TTL` (600s) → exit allow + cleanup file.
- **Verification:** Test fixture cho mỗi case (us3, us5). Cover acceptance_evals `us3-skip-when-autonomy-active` + `us5-no-op-when-no-clarification-suggested`.
- **Risk:** low

## T8 — Thêm escape token consume + log_fire vào enforcer

- **Touches:** `templates/claude/hooks/clarification_gate_enforcer.py`
- **Depends on:** T7
- **Acceptance:** Sau 3 skip path T7, kiểm `SKIP_REL`: tồn tại + chưa expired → đọc `reason`, `unlink(SKIP_REL)`, exit allow + `log_fire(bypass=[f"escape-token:{reason}"])`. Single-use consume.
- **Verification:** Test fixture: ghi `.skip_clarification_next.json`, chạy enforcer 1 lần → exit 0 + file unlinked. Chạy lần 2 cùng setup → no skip (state file đã consume). Covers `us4-skip-on-escape-token-single-use`.
- **Risk:** medium — file race condition nếu hook crash giữa read và unlink; chấp nhận (TTL 300s là fallback).

## T9 — Thêm shape check (4 markers) + enforce_mode integration vào enforcer

- **Touches:** `templates/claude/hooks/clarification_gate_enforcer.py`
- **Depends on:** T8
- **Acceptance:** Sau skip paths, grep response text cho 4 marker literal `UNDERSTANDING`, `ASSUMPTIONS`, `QUESTIONS`, `Searched:`. Thiếu ≥ 1 → list missing, gọi `get_enforce_mode(workspace, "clarification_gate_enforcer")` → `block` → exit 2 + stderr `[clarification-gate-enforcer] block: missing markers: <list>`; `warn` → exit 0 + stderr `[clarification-gate-enforcer] warn: ...`. `AGENT_TOOLKIT_STRICT=1` env override → block.
- **Verification:** Covers `us1-deny-stop-when-clarification-shape-missing` + `us2-allow-when-all-four-markers-present` + `us6-enforce-mode-and-strict-env-respected`.
- **Risk:** low

## T10 — Wire enforcer vào `templates/claude/settings.json` Stop chain

- **Touches:** `templates/claude/settings.json`
- **Depends on:** T9
- **Acceptance:** Thêm entry mới trong `Stop[].hooks[]`, vị trí SAU `evidence_audit.py` và TRƯỚC `debug_sentry.py`. Schema:
  ```json
  {
    "type": "command",
    "command": "{{ENV_PYTHON}} {{HOOKS_DIR}}/clarification_gate_enforcer.py",
    "timeout": 5
  }
  ```
- **Verification:** `jq '.hooks.Stop[].hooks | map(.command) | indices(... matches clarification_gate)' templates/claude/settings.json` returns position > position of evidence_audit, < position of debug_sentry. Covers us7 partial.
- **Risk:** low

## T11 — Thêm `clarification_gate_enforcer: "block"` entry vào `enforce_mode.example.json`

- **Touches:** `templates/agent_toolkit/enforce_mode.example.json`
- **Depends on:** none
- **Acceptance:** `per_hook.clarification_gate_enforcer = "block"` thêm vào template. `_doc` field update note "contract-enforcement hooks default block per ADR (pending)".
- **Verification:** `jq '.per_hook.clarification_gate_enforcer' templates/agent_toolkit/enforce_mode.example.json` returns `"block"`. Cover D8.
- **Risk:** low

## T12 — Tests: `TestUs1DenyWhenShapeMissing` class

- **Touches:** `tests/test_clarification_gate_enforcer.py` (new)
- **Depends on:** T9
- **Acceptance:** Class with 2 tests: (a) response = `"OK đã làm xong"` (text-only, no markers) → enforcer exit 2 + stderr chứa cả 4 marker thiếu; (b) response = `"UNDERSTANDING: ... ASSUMPTIONS: ..."` (2 markers only) → exit 2 + stderr chứa 2 marker thiếu (QUESTIONS, Searched:).
- **Verification:** `pytest tests/test_clarification_gate_enforcer.py::TestUs1DenyWhenShapeMissing -v` exits 0. Covers acceptance_eval `us1-deny-stop-when-clarification-shape-missing`.
- **Risk:** low

## T13 — Tests: `TestUs2AllowWhenComplete` class

- **Touches:** `tests/test_clarification_gate_enforcer.py`
- **Depends on:** T9
- **Acceptance:** Class with 1 test: response chứa đủ 4 marker literal → exit 0 + stderr empty + log_fire records `verdict='allow'`.
- **Verification:** `pytest tests/test_clarification_gate_enforcer.py::TestUs2AllowWhenComplete -v` exits 0. Covers `us2-allow-when-all-four-markers-present`.
- **Risk:** low

## T14 — Tests: `TestUs3SkipOnAutonomy` class

- **Touches:** `tests/test_clarification_gate_enforcer.py`
- **Depends on:** T7
- **Acceptance:** Class with 2 tests: (a) `.autonomy_active.json` fresh (ts = now) + response empty → exit 0 + `log_fire(bypass=["autonomy"])`; (b) `.autonomy_active.json` expired (ts = now - 24h) → enforcer KHÔNG skip, vẫn check shape.
- **Verification:** `pytest tests/test_clarification_gate_enforcer.py::TestUs3SkipOnAutonomy -v` exits 0. Covers `us3-skip-when-autonomy-active`.
- **Risk:** low

## T15 — Tests: `TestUs4EscapeTokenSingleUseAndReasonLength` class (~40 LOC, justified)

- **Touches:** `tests/test_clarification_gate_enforcer.py`
- **Depends on:** T1, T4, T8
- **Acceptance:** Class with 3 tests: (a) prompt `"skip-clarification: emergency-fix-prod"` → intent_router ghi state file; first enforcer run exits 0 + unlinks file + `log_fire bypass=["escape-token:emergency-fix-prod"]`; (b) second enforcer run cùng setup (file đã unlinked) → KHÔNG skip; (c) prompt `"skip-clarification: x"` (1 char) → intent_router KHÔNG ghi state file (regex `\S{8,200}` fail) → enforcer enforces shape normally.
- **Verification:** `pytest tests/test_clarification_gate_enforcer.py::TestUs4EscapeTokenSingleUseAndReasonLength -v` exits 0. Covers `us4-skip-on-escape-token-single-use`. **LOC exceeded justification**: 3 sub-cases (consume / single-use / reject-short) cần test cùng class để verify đầy đủ contract — split sẽ làm fragments thiếu invariant chung.
- **Risk:** medium — race condition T8, test phải mock đầy đủ.

## T16 — Tests: `TestUs5NoopWhenNoSuggestion` class

- **Touches:** `tests/test_clarification_gate_enforcer.py`
- **Depends on:** T7
- **Acceptance:** Class with 2 tests: (a) `.last_intent_suggested.json` absent → exit 0 silent + log_fire emit nothing; (b) state file tồn tại nhưng `skills` không chứa `"clarification-gate"` (vd: `["code-review"]`) → exit 0 silent.
- **Verification:** `pytest tests/test_clarification_gate_enforcer.py::TestUs5NoopWhenNoSuggestion -v` exits 0. Covers `us5-no-op-when-no-clarification-suggested`.
- **Risk:** low

## T17 — Tests: `TestUs6EnforceModeRespected` class

- **Touches:** `tests/test_clarification_gate_enforcer.py`
- **Depends on:** T9
- **Acceptance:** Class with 3 tests, setup chung (shape missing): (a) `enforce_mode.json` `clarification_gate_enforcer: "warn"` → exit 0 + stderr `[clarification-gate-enforcer] warn:`; (b) no entry (default fallback từ D8) → exit 2 (block); (c) `AGENT_TOOLKIT_STRICT=1` env + mode `warn` → exit 2 (strict override).
- **Verification:** `pytest tests/test_clarification_gate_enforcer.py::TestUs6EnforceModeRespected -v` exits 0. Covers `us6-enforce-mode-and-strict-env-respected`.
- **Risk:** low

## T18 — Tests: `TestUs7InstallerWiringOrder` class

- **Touches:** `tests/test_clarification_gate_enforcer.py`
- **Depends on:** T10
- **Acceptance:** Class with 1 test: mock install/update, parse downstream `settings.json`, assert `clarification_gate_enforcer.py` position in `Stop[0].hooks[]` > position of `evidence_audit.py` AND < position of `debug_sentry.py`.
- **Verification:** `pytest tests/test_clarification_gate_enforcer.py::TestUs7InstallerWiringOrder -v` exits 0. Covers `us7-installer-wires-hook-into-stop-chain`.
- **Risk:** low

## T19 — Bump `__version__` 0.12.3 → 0.13.0 trong `lib/installer.py`

- **Touches:** `lib/installer.py`
- **Depends on:** none
- **Acceptance:** `__version__ = "0.13.0"` (đổi từ `"0.12.3"`). Không thay đổi khác.
- **Verification:** `grep '^__version__' lib/installer.py` returns `__version__ = "0.13.0"`. `python -c "from lib.installer import __version__; assert __version__ == '0.13.0'"` exits 0.
- **Risk:** low

## T20 — Thêm CHANGELOG entry `## [0.13.0]`

- **Touches:** `CHANGELOG.md`
- **Depends on:** T19
- **Acceptance:** Entry mới ở top (sau line header), format khớp `## [0.12.3]` pattern (line đầu CHANGELOG). Content: ngày 2026-05-23, tên feature, list 3 module mới + 1 module patched, ref tới spec `specs/v0.13.0-clarification-gate-enforcer.md`, 1 dòng note "default `block` per D8".
- **Verification:** `head -10 CHANGELOG.md | grep "0.13.0"` returns 1 line. `grep -A 8 "^## \[0.13.0\]" CHANGELOG.md` includes ref to spec + module list.
- **Risk:** low

## T21 — Smoke verify: chạy full pytest suite + sample manual probe

- **Touches:** none (verification-only)
- **Depends on:** T12–T18
- **Acceptance:** (a) `/home/voducthang/NAKIVO/venv/bin/python -m pytest tests/test_clarification_gate_enforcer.py -v` → 7/7 test class pass (TestUs1..TestUs7); (b) manual probe: tạo mock `.last_intent_suggested.json` + chạy `clarification_gate_enforcer.py` với envelope chứa response thiếu marker → confirm exit 2 + stderr đúng format.
- **Verification:** Pytest output PASSED cho 7 class. Exit code shell snippet manual = 2. Đây là gate trước khi đi `/verify` upstream.
- **Risk:** low — if FAIL → fix specific test, không skip.

---

## Coverage

| User Story | Tasks covering | acceptance_eval cited |
|---|---|---|
| Story 1 (DENY shape missing) | T9, T12 | us1 → T12 ✓ |
| Story 2 (ALLOW shape complete) | T9, T13 | us2 → T13 ✓ |
| Story 3 (SKIP autonomy) | T7, T14 | us3 → T14 ✓ |
| Story 4 (SKIP escape token) | T1, T4, T5, T8, T15 | us4 → T15 ✓ |
| Story 5 (NO-OP no suggestion) | T7, T16 | us5 → T16 ✓ |
| Story 6 (enforce_mode + STRICT) | T9, T11, T17 | us6 → T17 ✓ |
| Story 7 (installer wiring) | T10, T18 | us7 → T18 ✓ |

- **Stories**: 7/7 ✓
- **acceptance_evals**: 7/7 cited ✓
- **No uncovered story · no unreached eval** ✓

## Out-of-tasks (mentioned in spec, deferred to manual follow-up)

- `/adr-add` cho policy "contract-enforcement hooks default block" (D8 note) — DEV chạy thủ công sau khi v0.13.0 land, không block /implement.
- `setup.py update /home/voducthang/NAKIVO` để NAKIVO nhận hook mới — DEV chạy sau khi merge upstream, không phải task implement.

## Sequencing recommendation cho `/implement`

Dependency graph:
```
T1 ─┐
T2 ─┴─ T3 ─┐
T4 ─── T5 ─┤
T6 ──────── T7 ──── T8 ──── T9 ──── T10 ──── T18
T11 ────────────────────────────── T17
T12 ─ T13 ─ T14 ─ T15 ─ T16 (parallel after T9/T7)
T19 ──── T20
T21 (final gate)
```

Parallelizable groups: {T1, T2, T6, T11, T19} có thể chạy đầu (no deps). Tests {T12-T18} chạy sau khi hook code xong (T9+T10).

Estimated wall-clock: ~20-30 phút autonomy (mỗi task ~1-2 phút Edit + verify).
