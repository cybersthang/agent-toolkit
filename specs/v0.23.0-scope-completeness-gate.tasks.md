---
tasks_for: scope-completeness-gate
status: done
created: 2026-05-27
last_updated: 2026-05-27
auto_chain_after_approval: true
---

# Tasks — scope-completeness-gate (R9)

> Derived from spec §5 Implementation Decisions (D1-D5) + 5 User Stories.
> Each task ≤30 LOC new/changed; Acceptance cites an acceptance_eval id;
> Verification is a concrete pytest node (TDD: test written WITH task).
> Total new-code budget ≈ 300 LOC (hook ~150 · patterns ~15 · tests ~120 · config/wire ~15).

## T1 — Add scope regex patterns to _patterns.py

- **Touches:** `templates/claude/hooks/_patterns.py` (extend)
- **Depends on:** none
- **Acceptance:** Covers `us3-resolution-markers`. 5 new compiled regex
  exist + match correctly: `SCOPE_DONE_RE` (`scope-done: S<N>`),
  `SCOPE_DEFER_RE` (`scope-defer: S<N> <reason>`), `SCOPE_CANT_RE`
  (`scope-cant: S<N> <reason>`), `DONE_FULL_CLAIM_RE` (done/full/xong/hoàn
  tất claim — mirror `DONE_CLAIM_GAP_RE`), `BYPASS_SCOPE_GATE_RE`
  (`bypass-scope-gate: <reason ≥8 chars>`). Reason groups capture text.
- **Verification:** `pytest tests/test_scope_completeness_gate.py::TestResolutionMarkers::test_markers_transition_status` exits 0.
- **Risk:** low — additive regex, mirror existing gap-gate patterns.

## T2 — Manifest derive from structured artifact + state schema

- **Touches:** `templates/cursor/skills/_common/scope-manifest/SKILL.md` (new),
  `templates/claude/hooks/implement_orchestrator.py` (extend — derive on autonomy start)
- **Depends on:** T1
- **Acceptance:** Covers `us1-manifest-derived-from-tasks`. On autonomy
  start, build `.agent-toolkit/.scope_manifest.json` from source priority
  `tasks.md` T<N> (priority 1) → `acceptance_evals` (priority 2). Each item
  `{id: S<N>, source, desc, status: pending}`. NO regex on DEV prompt
  (anti-requirement §7). Manifest lifecycle mirrors autonomy (D3): created
  here, TTL = autonomy `expires_at`.
- **Verification:** `pytest tests/test_scope_completeness_gate.py::TestManifestDerive::test_source_priority_tasks_then_evals` exits 0 + static grep confirms zero DEV-prompt trigger_patterns in source.
- **Risk:** medium — touches orchestrator; keep derive logic isolated in a helper, no behavior change when no tasks.md.

## T3 — Ad-hoc manifest auto-emit from TodoWrite ≥3

- **Touches:** `templates/claude/hooks/implement_orchestrator.py` (extend),
  `templates/cursor/skills/_common/scope-manifest/SKILL.md` (guide)
- **Depends on:** T2
- **Acceptance:** Covers `us5-adhoc-self-manifest`. When autonomy active +
  NO tasks.md + agent TodoWrite list has ≥3 items → auto-emit manifest
  (each todo = 1 item S<N>, priority 3 source). <3 items → no emit (silent).
  Sub-agent batch does NOT auto-trigger (D1: TodoWrite-only threshold=3).
- **Verification:** `pytest tests/test_scope_completeness_gate.py::TestAdhocManifest::test_todowrite_threshold_3` exits 0 (≥3 emits, 2 does not).
- **Risk:** medium — TodoWrite capture point; best-effort, must stay silent below threshold.

## T4 — scope_completeness_gate.py Stop hook (block semantics)

- **Touches:** `templates/claude/hooks/scope_completeness_gate.py` (new ~150 LOC)
- **Depends on:** T1, T2
- **Acceptance:** Covers `us2-gate-blocks-partial-done`. Reads
  `.scope_manifest.json`. If response matches `DONE_FULL_CLAIM_RE` AND ≥1
  item `status=pending` → emit envelope `{"decision": "block"}` + stderr
  lists incomplete S<N>. All items ∈ {done, deferred, cant} → exit 0.
  Resolution markers (T1 regex) flip item status. Reads tasks.md status
  directly (D4: `passed|failed|skipped` → done/pending). Default mode
  `block` (D2) via `get_enforce_mode`. Honors kill-switch
  `AGENT_TOOLKIT_DISABLE`. Whole-gate bypass `bypass-scope-gate:` (D5)
  via single-shot `.skip_scope_gate_next.json` (TTL 600s). `stop_hook_active`
  recursion guard.
- **Verification:** `pytest tests/test_scope_completeness_gate.py::TestBlockSemantics::test_block_when_pending_and_done_claim` exits 0 + source contains `"decision": "block"` + `get_enforce_mode`.
- **Risk:** medium — new blocking Stop hook; mirror gap_completeness_gate structure to minimize novelty.

## T5 — Activation boundary (silent when no manifest)

- **Touches:** `templates/claude/hooks/scope_completeness_gate.py` (extend T4)
- **Depends on:** T4
- **Acceptance:** Covers `us4-activation-boundary`. No `.scope_manifest.json`
  (autonomy off OR request <3 TodoWrite items, no tasks.md) → hook exit 0
  with ZERO stdout/stderr. Activation = autonomy active AND manifest exists.
- **Verification:** `pytest tests/test_scope_completeness_gate.py::TestActivation::test_silent_when_no_manifest` exits 0 (asserts empty output + exit 0).
- **Risk:** low — early-return guard; zero-friction is the default path.

## T6 — Wire into Stop chain + config + enforce_mode

- **Touches:** `templates/claude/settings.json` (wire),
  `.agent-toolkit/scope_gate.json` (new config: enabled, min_items=3, mode),
  `templates/agent_toolkit/enforce_mode.example.json` (add `scope_completeness_gate: block`),
  `tests/test_stop_chain_interactions.py` (update length 13→14 assertion)
- **Depends on:** T4, T5
- **Acceptance:** `scope_completeness_gate.py` wired into Stop chain AFTER
  `gap_completeness_gate` and BEFORE `verify_lint_scope` (which stays last).
  Stop chain length 13→14. Config file read by hook (absent → safe default).
- **Verification:** `pytest tests/test_stop_chain_interactions.py::TestStopChainOrder::test_verify_lint_scope_is_last tests/test_stop_chain_interactions.py::TestStopChainOrder::test_stop_chain_length` both exit 0.
- **Risk:** medium — shared file settings.json; verify_lint_scope MUST remain last (chain-order test enforces).

## T7 — Test scaffold + docs (hook count, CLAUDE.md row)

- **Touches:** `tests/test_scope_completeness_gate.py` (new ~120 LOC, 5 test classes),
  `README.md` (hook count + Why bullet), `templates/CLAUDE.md` (hook table row)
- **Depends on:** T1-T6
- **Acceptance:** All 5 test classes present (one per eval us1-us5).
  README hook count incremented (27→28 active hooks) + scope-gate Why
  bullet. CLAUDE.md enforcement table gains scope_completeness_gate row.
  Kill-switch test (`test_all_hooks_check_kill_switch`) still passes (new
  hook honors `AGENT_TOOLKIT_DISABLE`).
- **Verification:** `pytest tests/test_scope_completeness_gate.py tests/test_stop_chain_interactions.py` full pass + `pytest tests/` suite green (no regression).
- **Risk:** low — additive tests + docs; doc counts must match reality (R-audit lesson: count drift).

---

## Execution results (2026-05-27)

- T1 — status: passed (5 regex added; `test_markers_transition_status` green)
- T2 — status: passed (`test_source_priority_tasks_then_evals` green; derive consolidated into hook, see notes)
- T3 — status: passed (`test_todowrite_threshold_3_emits` + below-threshold-silent green; transcript-based capture, no new PostToolUse hook)
- T4 — status: passed (`test_block_when_pending_and_done_claim` green; blocks via exit code 2, mirrors gap gate)
- T5 — status: passed (`test_silent_when_no_manifest_*` green; zero output when no manifest)
- T6 — status: passed (Stop chain 13→14, `verify_lint_scope` still last; chain tests green)
- T7 — status: passed (12 scope tests + docs: hook count 27→30, test count →636, CLAUDE.md row)

Full suite: **636 passed** (`pytest tests/ --no-cov -q`).
