---
tasks_for: agent-resilience-supervisor
status: approved
created: 2026-05-27
last_updated: 2026-05-27
auto_chain_after_approval: true
---

# Tasks — agent-resilience-supervisor (v0.24.0)

> Re-scoped design: read-only stall-watcher + notify (cả extension+CLI) +
> CLI auto-relaunch (cap 10) + resume_state core (tái dùng R9 manifest).
> Mỗi acceptance_eval cited đúng 1 task; Verification = pytest node (TDD).
> Total new-code budget ≈ 600 LOC (resume_state ~80 · stall_watcher ~190 ·
> notify ~150 · session_brief extend ~40 · config ~20 · tests ~280 · docs).

## T1 — resume_state.py core (đọc R9 checkpoint → resume brief)

- **Touches:** `tools/resume_state.py` (new), `tests/test_resume_state.py` (new)
- **Depends on:** none
- **Acceptance:** Covers `us1-resume-state-core` + `us5-resume-idempotent`.
  `build_brief(workspace)` đọc `.scope_manifest.json` + tasks.md status +
  `.autonomy_active.json` → trả chuỗi liệt kê item `pending` + task hiện tại;
  task/item `done|passed` KHÔNG xuất hiện (idempotent). Thiếu manifest → None.
- **Verification:** `pytest tests/test_resume_state.py::TestResumeBrief::test_brief_from_manifest_tasks_autonomy` + `::TestIdempotent::test_passed_tasks_excluded` exit 0.
- **Risk:** low — read-only, tái dùng cấu trúc R9 đã có.
- **status: passed** (2026-05-27) — `_resume_state.py` (hooks/, lib underscore — deviation từ §3 `tools/`, note ở implement-notes); 4 tests xanh.

## T2 — stall_watcher detect (read-only) + resilience.json config

- **Touches:** `tools/agent_supervisor.py` (new — phần watch read-only),
  `.agent-toolkit/resilience.json` (new config), `tests/test_stall_watcher.py` (new)
- **Depends on:** T1
- **Acceptance:** Covers `us2-stall-detect-readonly`. Watcher chọn transcript
  `.jsonl` mtime mới nhất trong `~/.claude/projects/<encoded-proj>/` (D8);
  autonomy active + mtime cũ > `stall_seconds` (default 180, D7) HOẶC process
  chết (psutil, D6) → gọi notify (1 lần + cooldown D10). Transcript tươi →
  không notify. Read-only: path này KHÔNG có os.kill/terminate/relaunch.
- **Verification:** `pytest tests/test_stall_watcher.py::TestDetect::test_stale_transcript_triggers_notify` exit 0 + static: read-only path không chứa kill/terminate.
- **Risk:** medium — psutil dep (fallback mtime-only nếu thiếu); cross-platform mtime/process check.

## T3 — notify pluggable (toast + log + smtp + webhook)

- **Touches:** `tools/notify/` (new — dispatcher + 4 channel), `tests/test_notify.py` (new)
- **Depends on:** none
- **Acceptance:** Covers `us3-notify-pluggable`. Dispatcher đọc channels từ
  `resilience.json`; log luôn ghi `.agent-toolkit/.stall_alert.json`; toast
  (Win PowerShell / Linux notify-send theo `os.name`); smtp + webhook đọc
  creds từ env (`.codex/mcp.local.env`), KHÔNG hardcode secret.
- **Verification:** `pytest tests/test_notify.py::TestChannels::test_each_channel_payload_and_creds_from_env` exit 0 + static grep: zero secret literal.
- **Risk:** medium — smtp/webhook mock-only trong test (không gửi thật); credentials policy.

## T4 — CLI auto-relaunch (--relaunch, cap 10 + backoff)

- **Touches:** `tools/agent_supervisor.py` (extend — `--relaunch` mode),
  `tests/test_stall_watcher.py` (extend)
- **Depends on:** T1, T2, T3
- **Acceptance:** Covers `us4-cli-relaunch-cap`. `--relaunch` mode (CLI-only):
  khi `claude` exit-fail/treo → relaunch `claude -c -p "<resume_state brief>"`
  tối đa 10 lần + exponential backoff; lần 11 KHÔNG relaunch → notify DEV
  "cạn cap". Mode mặc định (không `--relaunch`) = notify-only.
- **Verification:** `pytest tests/test_stall_watcher.py::TestCliRelaunch::test_cap_10_then_notify` exit 0 (mock claude subprocess).
- **Risk:** medium — subprocess mock; backoff timing; CLI-only path.

## T5 — session_brief resume-brief injection (extension semi-auto)

- **Touches:** `templates/claude/hooks/session_brief.py` (extend `_format_autonomy`)
- **Depends on:** T1
- **Acceptance:** Khi autonomy active + manifest có pending, SessionStart brief
  thêm 1 block "🔄 RESUME — đang dở T<i>, pending S<n>, gõ 'tiếp'" (từ
  resume_state). Verify thực nghiệm SessionStart-fire-on-resume (D9); fallback:
  manifest-on-disk vẫn cho agent đọc nếu hook không fire on resume.
- **Verification:** `pytest tests/test_session_brief.py::test_resume_block_when_pending` exit 0 (seed manifest+autonomy → assert brief chứa RESUME block). Hook-on-resume = manual/CI note trong implement.
- **Risk:** low — extend hook hiện có; không block path.

## T6 — Cross-platform hardening + docs + 529 config

- **Touches:** `README.md`, `templates/CLAUDE.md`, `docs/resilience.md` (new),
  `requirements.txt` (psutil), ensure 3 test file
- **Depends on:** T1-T5
- **Acceptance:** Covers `us6-cross-platform`. Watcher/notify/relaunch dùng API
  platform-agnostic (psutil; toast nhánh theo os.name; không hardcode /bin hay
  PowerShell-only ngoài nhánh Win). `docs/resilience.md` + README mục
  `CLAUDE_CODE_MAX_RETRIES` (giảm 529 từ gốc) + cách dùng watcher 2 mode.
  CLAUDE.md ghi tool mới + resume-brief.
- **Verification:** `pytest tests/test_stall_watcher.py tests/test_notify.py tests/test_resume_state.py` xanh (CI matrix Win+Ubuntu) + `pytest tests/` no regression.
- **Risk:** low — docs + hardening; doc số liệu phải khớp (count drift lesson).

---

## Execution results (2026-05-27)

- T1 — passed (`_resume_state.py` hooks/ lib; 4 tests). Note: lib trong hooks/ thay vì tools/ (§3) để session_brief import sạch + kill-switch test skip.
- T3 — passed (`tools/notify.py` 4 channel stdlib; 3 tests; creds-from-env). [reorder: làm trước T2 vì T2 phụ thuộc notify]
- T2 — passed (`tools/agent_supervisor.py` detect read-only + `resilience.example.json`; 6 tests; static read-only contract).
- T4 — passed (`relaunch_loop` cap 10 + backoff → notify; 3 tests).
- T5 — passed (`session_brief._format_resume`; 2 tests).
- T6 — passed (`docs/resilience.md` + README v0.24 + CLAUDE_CODE_MAX_RETRIES; cross-platform branches; 663 full-suite green).

Full suite: **663 passed** (`pytest tests/ --no-cov -q`). Fixed `test_hook_crash_wrapper` để skip `_`-prefixed lib modules.
