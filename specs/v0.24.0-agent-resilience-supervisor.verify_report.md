# Verify Report — agent-resilience-supervisor (v0.24.0)

- **Date**: 2026-05-27
- **Spec**: `specs/v0.24.0-agent-resilience-supervisor.md`
- **feature_kind**: contract (idempotent resume guarantee — no classification
  real-data-proof required; Step 1.8 N/A)
- **Verdict**: ✅ **VERIFIED** — 6/6 acceptance_evals PASS, 0 GAP, 0 BLOCKER
- **Probe layer**: `log` / unit (pytest with mocked I/O). This is toolkit-
  internal Python infra; MCP `realdata_test`/`postgres` N/A.

## Acceptance eval results

| Eval | Story | Probe (pytest) | Result |
|---|---|---|---|
| us1-resume-state-core | S1 | `test_resume_state.py::TestResumeBrief` (3) | ✅ PASS |
| us5-resume-idempotent | S5 | `test_resume_state.py::TestIdempotent` (1) | ✅ PASS |
| us2-stall-detect-readonly | S2 | `test_stall_watcher.py::TestDetect` (6, incl. read-only static contract) | ✅ PASS |
| us4-cli-relaunch-cap | S4 | `test_stall_watcher.py::TestCliRelaunch` (3) | ✅ PASS |
| us3-notify-pluggable | S3 | `test_notify.py::TestChannels` (3) | ✅ PASS |
| us6-cross-platform | S6 | 3 test files green + docs present | ✅ PASS |

Plus T5 extension consumer: `test_session_brief.py` (2) — RESUME block injected
when manifest pending, absent when all done.

## Regression

- **Full suite**: `pytest tests/ --no-cov -q` → **663 passed**, 0 failed
  (was 645; +18 from the 4 new test files).
- Fixed `test_hook_crash_wrapper.py` to skip `_`-prefixed lib modules
  (so `_resume_state.py` lib isn't treated as a hook).
- Hook count unchanged (30) — `_resume_state` is a lib; supervisor + notify
  are `tools/`, not hooks.

## ⚠️ Verification scope limit (honest disclosure)

What pytest verifies = the **logic** with mocked boundaries:
- resume_state brief building + idempotency (real files, real logic). HIGH.
- stall detection math, cooldown, read-only contract, relaunch cap/backoff
  (mocked `run_claude`/`sleep`/`notify`). HIGH for the decision logic.
- notify channel payloads + creds-from-env (mocked smtplib/urllib/subprocess). MEDIUM.

What pytest does NOT verify (cannot, in this sandbox):
- A real Windows toast / macOS / Linux `notify-send` actually popping up.
- A real SMTP send / real Slack/Discord webhook delivery.
- Detecting a REAL hung live `claude` session and relaunching it end-to-end.
- Whether `SessionStart` fires on a real VSCode-extension *resume* (spec D9 —
  flagged for empirical check; manifest-on-disk fallback covers the gap).

→ These need a real environment. The unit tests prove the logic is correct;
**field-verify the toast/SMTP/webhook + live-hang path on the target machine
before relying on them in production.**

## Deviations (→ implement-notes)

1. `_resume_state.py` placed in `templates/claude/hooks/` (lib, underscore)
   rather than `tools/` (spec §3) — so `session_brief` imports it cleanly and
   the kill-switch test skips it.
2. Task order: implemented T3 (notify) before T2 (watcher) since T2 depends on
   notify. Same coverage.
3. No `requirements.txt` created (repo has none); `psutil` documented as an
   optional dep with graceful degradation instead.
