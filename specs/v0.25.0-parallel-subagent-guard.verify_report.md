# Verify Report — parallel-subagent-guard (v0.25.0)

- **Date**: 2026-05-28
- **Spec**: `specs/v0.25.0-parallel-subagent-guard.md`
- **feature_kind**: contract (file-disjoint guarantee — no classification
  real-data-proof required; Step 1.8 N/A)
- **Verdict**: ✅ **VERIFIED** — 7/7 acceptance_evals PASS, 0 GAP, 0 BLOCKER
- **Probe layer**: `log` / unit (pytest with mocked envelope) +
  `regression` (cross-platform matrix). MCP `realdata_test`/`postgres`
  N/A — toolkit-internal Python infra.

## Acceptance eval results

| Eval | Story | Probe (pytest) | Result |
|---|---|---|---|
| us1-parallel-batching-skill | S1 | `test_parallel_conflict_guard.py::TestSkill::test_skill_md_has_template` | ✅ PASS |
| us2-wave-manifest-schema | S2 | `TestWaveCli` (4 tests) | ✅ PASS |
| us3-conflict-guard-block | S3 | `TestGuard` cross-zone tests (sub↔sub + main↔sub D8) | ✅ PASS |
| us4-same-owner-allow | S4 | `TestGuard::test_same_owner_allow` | ✅ PASS |
| us5-outside-zone-silent | S5 | `TestGuard::test_outside_zone_silent` + `test_no_manifest_silent` | ✅ PASS |
| us6-manifest-lifecycle | S6 | `TestLifecycle` (4 tests: wave_done, TTL, clear, bypass) | ✅ PASS |
| us7-cross-platform | S7 | 19/19 PASS Win 3.8 + WSL Linux 3.12 | ✅ PASS |

Plus T1 `TestBypass::test_token_capture_and_consume` — intent_router writes
the `.skip_parallel_guard_next.json` token on `bypass-parallel-guard:` prompt.

## Regression

- **Full suite**: `pytest tests/ --no-cov -q` → **687 passed**, 0 failed
  (was 663 pre-v0.25; +24 = 19 new + 5 incidental from earlier session).
- **PreToolUse chain**: `test_pretool_chain` still asserts `invariant_guard`
  first + `implement_snapshot_hook` last; our hook inserted in the middle
  preserves order.
- **Hook count**: 30 → 31 (`parallel_conflict_guard.py` added).
- **Cross-platform** verified locally on WSL Ubuntu (lesson from v0.24
  dispatch monkeypatch bug — never trust Windows-only green).

## Linux-cross-check (lesson from v0.24)

In v0.24 a dispatch test passed on Windows local but failed on Linux CI
due to a cached-function-reference bug. This time I ran the full
test_parallel_conflict_guard.py on WSL Ubuntu 24.04 (Python 3.12) BEFORE
declaring verified — 19/19 PASS. The hook uses platform-neutral primitives
only (Path.resolve, fnmatch, json, subprocess).

## ⚠️ Verification scope limit (honest disclosure)

What pytest verifies = the hook logic + CLI with mocked envelopes (no real
sub-agent fan-out happens during the test). HIGH confidence on:
- Manifest emit/parse/clear lifecycle (real files).
- Cross-zone decision rule (real envelope JSON, mocked subprocess).
- Smart-match (exact / dir-prefix / glob) on synthetic paths.
- Bypass token capture + consume (real file write).

What pytest does NOT verify (cannot, in this sandbox):
- A real `Agent` tool fan-out where the envelope's `agent_id` actually
  comes from Claude Code (the test FAKES it). Per docs hooks.md the field
  IS there for sub-agents, but the field-shape is **assumed** — needs
  empirical verify on a real parallel sub-agent run.
- Whether PreToolUse hooks fire AT ALL for sub-agent tool calls (docs
  gap, flagged in spec D1) — if they don't, the guard silently doesn't
  activate for sub-agent edits (still works for main-agent cross-zone).

→ Field-verify on a real parallel run before relying on it in production.

## Deviations (→ implement-notes)

1. Inserted hook into PreToolUse `Edit|Write|MultiEdit|NotebookEdit`
   matcher (sau invariant_guard) — spec §3 listed settings.json edit
   without exact position; chose right-after-invariant_guard (semantic
   sibling) to keep the gate-cluster contiguous.
2. CLI helper `parallel_wave.py` placed in `tools/` (matches v0.24
   `agent_supervisor.py` precedent) — installed via toolkit, not via
   consumer's `.claude/` dir.
3. PreToolUse block envelope uses `permissionDecision: deny` (new
   Claude Code envelope format) instead of the older exit-code-2 approach
   used by Stop-chain blockers like scope_completeness_gate. Matches
   PreToolUse contract per hooks.md.
