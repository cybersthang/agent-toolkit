# Verify Report — sub-agent-stall-watcher (v0.26.0)

- **Date**: 2026-05-28
- **Spec**: `specs/v0.26.0-sub-agent-stall-watcher.md`
- **feature_kind**: contract (multi-transcript stall-detection invariant)
- **Verdict**: ✅ **VERIFIED** — 7/7 acceptance_evals PASS, 0 GAP, 0 BLOCKER
- **Probe layer**: `log` / unit (pytest, mocked notify.dispatch + synthetic
  transcript files via os.utime). MCP `realdata_test`/`postgres` N/A.

## Acceptance eval results

| Eval | Story | Probe (pytest) | Result |
|---|---|---|---|
| us1-multi-transcript-discovery | S1 | `TestDiscovery` (2 tests) | ✅ PASS |
| us2-per-subagent-stall-detect | S2 | `TestDetect` (2 tests, independent + cooldown) | ✅ PASS |
| us3-notify-identifies-which-subagent | S3 | `TestNotifyPayload::test_payload_fields` | ✅ PASS |
| us4-activation-boundary | S4 | `TestActivation` (4 tests: no-manifest / wave-done / TTL / autonomy-off) | ✅ PASS |
| us5-lifecycle-integrate | S5 | `TestLifecycle` (2 tests: main-path independent + multi-mode inactive after wave-done) | ✅ PASS |
| us6-cross-platform | S6 | 13/13 PASS Win 3.8 + WSL Linux 3.12 | ✅ PASS |
| us7-notify-only-no-relaunch | S7 | `TestNoRelaunch` (static check + spy on relaunch_loop) | ✅ PASS |

## Regression

- **Full suite**: `pytest tests/ --no-cov -q` → **700 passed**, 0 failed
  (was 687 pre-v0.26; +13 v0.26 tests).
- **v0.24 tests** (`test_stall_watcher.py`): no regression — main path
  unchanged.
- **v0.25 tests** (`test_parallel_conflict_guard.py`): no regression —
  this spec READS the manifest, never writes/changes it.
- Hook count unchanged (31) — v0.26 extends `tools/agent_supervisor.py`,
  not a new hook.

## Linux cross-check

Same lesson as v0.24/v0.25: ran the v0.26 test file on WSL Ubuntu 3.12
BEFORE declaring verified. 13/13 green. Uses Path.glob + os.utime
(cross-platform stdlib).

## ⚠️ Verification scope limit (honest disclosure)

Tests use SYNTHETIC sub-agent `.jsonl` files (os.utime to set mtime). HIGH
confidence on:
- Discovery filter (mtime > created_ts AND not main).
- Per-transcript stall detection + per-transcript cooldown.
- Notify payload field shape + prefix.
- Activation 4-condition guard.
- Sub-agent path = no kill/Popen/relaunch (static + spy).

What pytest does NOT verify (cannot, in this sandbox):
- A REAL parallel Agent-tool fan-out where Claude Code actually writes
  multiple `<session-id>.jsonl` files in `~/.claude/projects/<encoded>/`.
  Docs (sessions.md) say it does, but path encoding + concurrent-write
  contention are unverified empirically.
- Whether a real hung sub-agent transcript truly stops advancing on disk
  (vs being flushed by some periodic checkpoint we don't see).

→ Field-verify on a real parallel wave + induced hang on the target
machine before relying on it in production.

## Deviations (→ implement-notes)

1. Tests use `projects_root` override param (added in v0.24 already) so
   we don't touch real `~/.claude/projects/`.
2. `notify.py:_alert_title` extended with `kind="sub-agent"` branch; old
   `kind` absence still produces the legacy title (backward-compat).
3. T3 + T7 evals folded into the same files as T2 + T6 (no separate test
   files / docs files) — minor; same coverage.

## Closes gap

| Pre-v0.26 | Now |
|---|---|
| Sub-agent treo giữa wave: v0.24 không bắt (chỉ main session) | ✅ v0.26 multi-transcript bắt được |
| v0.25 §7 "khong kết hợp v0.24 ở Phase 1" | ✅ v0.26 chính là Phase 2 integration |
