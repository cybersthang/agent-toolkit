# Implement Notes — v0.31.0 independent-review-subagent

## §1 Scope deviations (vs tasks.md)
- **T8** không làm thành "hook phần D" — feed-to-`.open_gaps.json` thực hiện ở
  **skill** (T3 step 5): agent có findings nên agent ghi gap; gate chỉ VERIFY.
  Kiến trúc đúng hơn nhưng lệch tasks.
- **Spec-location bug PHÁT HIỆN + FIX khi viết T11**: gate `_verified_spec` đọc
  `.agent-toolkit/specs/` còn CLI + repo dogfood dùng `specs/` → gate không
  trigger ở layout dogfood. Đã sửa cả gate + CLI check CẢ 2 location. (Đúng loại
  bug feature này sinh ra để bắt — bắt được nhờ viết test thật.)

## §2 In-transcript trade-offs
- Gate lấy review-sha bằng **shell sang CLI** (single source ID-18) thay vì
  re-implement hash → tránh drift, đổi lại chi phí subprocess git trong Stop
  timeout (đặt 15s, ID-22).
- packet-purity = **size-ratio loose** (`≤ tol*psize*4`) — heuristic thô (B1
  residual), đủ bắt nhét-context lớn, không chính xác.

## §3 Open follow-ups
- **F1** Field `"mode"` trong config KHÔNG được gate dùng (gate đọc enforce_mode).
  Bỏ field hoặc cho gate đọc — chốt 1 nguồn (tránh drift).
- **F2** Positive-path integration test (live Task sub-agent thật consume packet)
  chưa có — chỉ unit-level.
- **F3** Packet-purity → đo token-count thực thay vì size×4.
- **F4** Phase 2: multi-lens + model-diversity (giảm B2 same-model blind-spot).
- **F5** `_reviewer_evidence` glob bám layout v0.27 — đổi layout sẽ miss.

## §4 Confidence
| Hạng mục | Conf | Ghi chú |
|---|---|---|
| CLI emit-context + sha (T1) | **H** | deterministic test pass |
| Gate decision logic (T5-7) | **H** | 11 test e2e (tmp git) pass |
| Reviewer-evidence positive path | **L** | chỉ unit; chưa live-spawn (F2) — verify trước khi bật strict prod |
| Packet-purity (anti-leak) | **L** | heuristic thô (F3) |
| Convergence/jam-escape | **M-H** | jam_escape test pass; oscillation chưa test riêng |

## §5 Self-review round (feature reviewed ITSELF — dogfood, 2026-05-30)
Ran the `independent-review` flow on v0.31.0 itself (emit-context → fresh-context
reviewer sub-agent). The reviewer found **3 BLOCKER + 4 MEDIUM + 2 LOW that the
implementation + 11 passing tests had MISSED** — proving the feature's premise
(independent fresh-context review catches what same-context misses) AND closing
F2 (live positive-path spawn works). All verified real + FIXED:
- **B1** escalate never wrote terminal verdict → re-escalate loop. FIX: write
  `verdict="escalated"` + short-circuit early.
- **B2** `emit_stop_context` exits → strict-mode ceiling-escalate dead-coded the
  block. FIX: escalate emits per-mode (strict→block once), single exit.
- **B3** `normalize_for_hash` didn't neutralize comment-only diffs. FIX: strip
  git-meta + diff-prefix → comment-only stable, code-change changes sha. +regression test.
- **M1** `non_progress_streak` dead config. FIX: per-spec counter + blocker-
  fingerprint oscillation detection; round counts review-CYCLES (sha change).
- **M2** packet-purity measured whole transcript ×4 (arbitrary). FIX: measure
  the spawn prompt (first-user-msg) size ≤ `reviewer_prompt_max_bytes` (anti-leak).
- **M3** sub-agent scan not session-scoped → cross-session stale could giả-pass.
  FIX: scope glob to `<session>/subagents/`.
- **M4** positive `_reviewer_evidence` path untested. FIX: added in-process test
  (monkeypatch home) covering clean→True / bloated→False / cross-session→False.
- **LOW** dead `mode` config field removed; `prune_related` stdlib-walk filter added.
Result: 14 gate tests, 1015 repo suite, ruff clean. Closes F1/F2/F3/F5 from §3.

## §6 Self-review round 2 — CONVERGED (2026-05-30)
Re-ran the independent reviewer on the post-fix code (new review-sha 148d656a ≠
round-1 87f59ecc → fresh cycle, as the convergence model intends).
- **VERDICT: CONVERGED (0 BLOCKER)** — reviewer confirmed all 7 round-1 issues
  (B1/B2/B3/M1/M2/M3/M4) genuinely fixed with file:line proofs. Convergence
  terminated at round 2 (< cap 5). This is the full review→fix→re-review loop,
  demonstrated on the feature itself, reaching the 0-BLOCKER criterion.
- **R2-MEDIUM (fixed):** sha-consumption check false-matched on the packet's
  Read tool_result → weakened ID-12. FIX: require the sha in an ASSISTANT echo,
  not anywhere in the transcript. +regression test
  (`test_reviewer_evidence_requires_assistant_echo_not_tool_result`).
- **R2-LOW (accepted, narrow):** (1) full sub-agent transcript scan has no size
  cap on the Stop path (latency only; bounded by session sub-agent count);
  (2) escalate leaves the old sha verdict="escalated" so a byte-identical revert
  allows done, and seen_fp reset blinds oscillation post-escalate. Both narrow
  (reviewer's own doubt-pass rated LOW) → follow-up, not blocking.
Final: 15 gate tests · 1016 repo suite · ruff clean.
