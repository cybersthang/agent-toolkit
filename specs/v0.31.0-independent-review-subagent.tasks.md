# tasks — v0.31.0 independent-review-subagent

> Emit bởi /clarify→/tasks (2026-05-30). STOP — DEV review trước `/implement`.
> Quy ước: mỗi task ≤ ~30 LOC ròng. Eval cited: mỗi `acceptance_evals` entry
> gắn ĐÚNG 1 task. Mọi User Story phủ bởi ≥1 task.

## T1 — CLI `emit-context` (packet + packet-sha)  ·  [eval: us2-independence-packet-code-assembled]
- **Touches:** `tools/independent_review.py` (mới).
- **Depends on:** —
- **Acceptance:** `python tools/independent_review.py emit-context <spec>` gom
  diff feature-scope + spec + acceptance_evals + invariants liên quan → packet
  file + in `packet_sha`. Cùng diff ⇒ cùng sha (deterministic). Bám pattern
  `tools/parallel_wave.py:63` (argparse + json.dump).
- **Verification:** `pytest -k packet_sha` (determinism); env/venv shebang (no bare python).
- **Risk:** sha không ổn định nếu gom theo thứ tự file không sort → sort trước hash.

## T2 — Config + state schema  ·  [story: us4]
- **Touches:** `templates/agent_toolkit/independent_review.example.json` (mới).
- **Acceptance:** khai báo `enabled · max_rounds(3) · reviewer_count(1) ·
  model(inherit) · risk_globs · skip_trivial_loc(200) · mode(warn)`. State
  runtime `.agent-toolkit/.independent_review.json` key theo diff-sha: findings,
  verdict, round, blocker_fingerprints.
- **Verification:** `pytest -k config_schema` (load/default).
- **Risk:** default sai → gate quá nhạy; chốt theo ID-7..ID-11.

## T3 — Skill `independent-review/SKILL.md`  ·  [story: us2]
- **Touches:** `templates/cursor/skills/_common/independent-review/SKILL.md` (mới).
- **Depends on:** T1.
- **Acceptance:** contract: agent spawn reviewer (Task tool) với CHỈ packet path;
  reviewer "review only from packet"; schema finding (BLOCKER/MEDIUM/LOW + Proof
  + Doubt-pass); luật hội tụ (ID-4). Reuse `code-review`+`doubt-driven-review`+`parallel-batching`.
- **Verification:** `pytest -k skill_metadata_valid` (test có sẵn).
- **Risk:** prompt lỡ chèn reasoning → mất độc lập (ID-2 residual) → nhấn "only packet".

## T4 — Command `/review-independent` (manual path)  ·  [story: ID-13]
- **Touches:** `templates/claude/commands/review-independent.md` (mới).
- **Depends on:** T3.
- **Acceptance:** lệnh manual on-demand gọi skill independent-review trên scope
  chỉ định. Ghi rõ: flow thường KHÔNG cần gõ (gate auto-drive).
- **Verification:** review thủ công + `pytest -k command_present`.
- **Risk:** trùng vai với gate → doc phân định rõ manual vs auto.

## T5 — Hook: done-detect + skip-trivial + sha-cache  ·  [eval: us4-token-skip-trivial-and-cache]
- **Touches:** `templates/claude/hooks/independent_review_gate.py` (mới, phần A).
- **Depends on:** T2.
- **Acceptance:** Stop hook: phát hiện done-claim trên spec-tracked file (reuse
  `post_edit_verify_gate._file_in_implementing_spec`, ID-8); diff ≤200 LOC /
  không feature-scope (regex `auto_test_runner.py:55`, ID-7) → verdict `skipped`;
  diff-sha == lần review trước → `cached` (không spawn).
- **Verification:** `pytest -k skip_or_cache`.
- **Risk:** sha scope sai → false-stale; theo ID-9 (feature-scope file-set).

## T6 — Hook: verify 3-lớp bằng chứng  ·  [eval: us1-gate-blocks-done-without-review]
- **Touches:** `independent_review_gate.py` (phần B).
- **Depends on:** T1, T5.
- **Acceptance:** chặn done trừ khi: (a) `tool_use` Task thật trong cycle, (b)
  sub-agent transcript tồn tại (reuse `agent_supervisor.discover_sub_agent_transcripts`)
  + chứa **packet-sha literal** (ID-12), (c) artifact khớp diff-sha + findings
  trỏ file:line có thật. WARN default; strict→block (enforce_mode/ENV). Không tin prose.
- **Verification:** `pytest -k blocks_done`.
- **Risk:** discover layout đổi → reuse bản đã field-verified v0.27.

## T7 — Hook: convergence + escalate  ·  [eval: us3-convergence-terminates]
- **Touches:** `independent_review_gate.py` (phần C).
- **Depends on:** T6.
- **Acceptance:** hội tụ 0-BLOCKER (MEDIUM/LOW chỉ báo); giảm đơn điệu +
  finding-fingerprint chống dao động; cap 3 vòng; hết cap/oscillation → escalate
  `gap-cant-fix`. Mang `stop_hook_active` + `_RECURSION_HARD_CAP` (bám
  `evidence_audit.py:33-41`).
- **Verification:** `pytest -k convergence`.
- **Risk:** cap bị strip → loop ⇒ phủ bởi invariant T10.

## T8 — Feedback: BLOCKER → `.open_gaps.json`  ·  [story: us1]
- **Touches:** `independent_review_gate.py` (phần D) — ghi gap.
- **Depends on:** T6.
- **Acceptance:** BLOCKER xác nhận → append vào `.open_gaps.json` (status open) →
  `gap_completeness_gate` tự ép fix. Không xây tracker mới.
- **Verification:** `pytest -k feeds_open_gaps`.
- **Risk:** double-count với gap gate → dedup theo finding-fingerprint.

## T9 — Đăng ký + fail-open wrapper  ·  [story: us1]
- **Touches:** `templates/claude/settings.json`, dogfood `.claude/settings.json`,
  `templates/CLAUDE.md`, `enforce_mode*.example.json`.
- **Depends on:** T7.
- **Acceptance:** gate vào Stop chain (sau verify_lint, trước debug_sentry);
  `run_main_safe` fail-open wrapper (invariant hooks-fail-open); per_hook entry
  (warn default, strict block); 1 dòng bảng Enforcement CLAUDE.md.
- **Verification:** `pytest -k settings or -k hook_crash_wrapper`.
- **Risk:** thứ tự Stop chain sai → đo nhầm; đặt sau verify_lint.

## T10 — Invariant cấm strip hard-cap (C4)  ·  [story: us3]
- **Touches:** `.agent-toolkit/invariants.json` (+ gợi ý DEV `/inv-add`).
- **Depends on:** T7.
- **Acceptance:** invariant blocker: `independent_review_gate.py` phải giữ
  `stop_hook_active` + hard-cap (must_keep_regex). invariant_guard DENY edit strip.
- **Verification:** `pytest -k merge_invariants`.
- **Risk:** regex quá rộng → false-deny; scope đúng file.

## T11 — Tests `test_independent_review_gate.py`  ·  [phủ us1-us4]
- **Touches:** `tests/test_independent_review_gate.py` (mới).
- **Depends on:** T5,T6,T7,T8.
- **Acceptance:** markers `blocks_done · packet_sha · convergence · skip_or_cache
  · feeds_open_gaps · fail_open` — assert đúng 4 expected của acceptance_evals.
- **Verification:** `make rebuild` xanh (ADR-002 real-data proof).
- **Risk:** test dùng envelope giả thiếu field → dùng transcript tổng hợp thật (bài học R8/gate cũ).

## T12 — Docs + ADR  ·  [story: tất cả]
- **Touches:** `CHANGELOG.md`, `.agent-toolkit/decision-log.md` (gợi ý `/adr-add`),
  README/HOOK_CHAIN.
- **Depends on:** T9.
- **Acceptance:** CHANGELOG v0.31.0; ADR "review độc lập ở done-boundary, verify
  không tin prose"; HOOK_CHAIN thêm gate; bump version.
- **Verification:** `pytest -k version_bump`.
- **Risk:** doc drift (bài học eval) → single-source version.

---
**Thứ tự đề xuất:** T1→T2→T3→T4 (mặt phẳng artifact) · T5→T6→T7→T8 (hook lõi) ·
T9→T10 (đăng ký+invariant) · T11 (test) · T12 (docs). 
**Eval coverage:** us1→T6 · us2→T1 · us3→T7 · us4→T5 (mỗi eval đúng 1 task). 
**Story coverage:** us1→T6/T8/T9 · us2→T1/T3 · us3→T7/T10 · us4→T5/T2.

---
## Amendments — gap-fix B1–B4 (2026-05-30)
- **T3 (+ID-15):** skill prompt reviewer = đối kháng "mặc định nghi ngờ, cố bác
  bỏ từng hunk; không giả định implementer đúng".
- **T5 (+ID-16):** trigger done-boundary theo STATE (spec `status: verified` +
  tasks.md hết pending + diff feature-scope), KHÔNG regex done-text.
- **T6 (+ID-14):** verify packet-purity (packet-sha + size ≤1.2× → else
  `tainted-review`); **(+ID-17)** mang `stop_hook_active`+hard-cap, N-block /
  reviewer-fail → degrade WARN + escalate DEV (jam-escape).
- **T7 (+ID-17):** cap + escalate là terminal-state; thêm test `jam_escape`.
- **T11:** thêm markers test `tainted_review`, `adversarial_prompt`,
  `state_trigger`, `jam_escape`.

## Amendments — gap-fix M1–M5 (2026-05-30)
- **T1 (+ID-18):** sha = hash(diff + spec + evals + invariants), normalize bỏ
  comment/whitespace. **(+ID-19):** packet prune sẵn file liên quan (import/inherit).
- **T3 (+ID-19):** skill cap `max_reads_per_round`(10); vượt → note `context-limited`.
- **T6 (+ID-21):** chỉ feed BLOCKER đã chứng-minh-được; chứng-minh-fail → MEDIUM.
  **(+ID-22):** đọc tail transcript cap 256KiB; chỉ ĐỌC artifact, không đợi sub-agent.
- **T7 (+ID-20):** non-converge = finding tái xuất HOẶC regression vùng-fix; blocker
  mới vùng khác không tính, nhưng trần tuyệt đối 5 vòng.
- **T8 (+ID-21):** chỉ bơm blocker confirmed vào `.open_gaps.json`.
- **T9 (+ID-22):** timeout riêng cho gate = 15s.
- **T11:** thêm markers test `diff_sha_contract`, `token_cap`, `regression_vs_new`,
  `hallucinated_blocker_downgraded`, `tail_cap`.
