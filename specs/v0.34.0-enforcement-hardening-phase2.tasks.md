# tasks.md — v0.34.0 enforcement-hardening-phase2

Spec: `specs/v0.34.0-enforcement-hardening-phase2.md` (status: clarified)
Decisions: Q1=block-default ALL installs · Q2=FULL scope · Q3=new hook · Q4=per-trigger flip
Directive: "cover hết rủi ro để an toàn nhất" → every task ships **block-CAPABLE @ WARN** + convergence-cap; block-default flip is post-FP≈0.

> DEV review gate: eyeball this list, then `/implement enforcement-hardening-phase2`.
> Ordered by dependency. Each task is small + independently verifiable.

## Phase 0 — prerequisites (must land first)

**T1 · F1.5 unwrap launchers** `[~25 LOC]`
- Touches: `templates/claude/hooks/verify_lint.py` (`_runs_tests`).
- Do: split off known launchers (`poetry|uv|pdm|hatch run`, `xvfb-run`, `env`, `nice`, `timeout <n>`, `stdbuf`, `ionice`) → recurse on wrapped cmd.
- Acceptance: `ev1b-wrapped-runner-counts`. Verification: subprocess test `poetry run pytest`+"5 passed"→counts; `echo pytest`→still NOT.
- Risk: over-broad unwrap re-admits `echo` → adversarial-FP review (R5.3).

**T2 · R5.1 shared convergence-cap helper** `[~30 LOC]`
- Touches: `templates/claude/hooks/_common.py` (new `converge_or_degrade(state, key, cap)` reusing the independent_review_gate pattern).
- Do: block_streak counter + degrade-to-warn + escalate when ≥ cap.
- Acceptance: `ev6-convergence-no-deadlock`. Verification: gate fired cap+1× → degrades.
- Risk: cap mis-set → too-loose/too-tight; default cap=3 (match evidence_audit).

## Phase 1 — ① spec-flow (block-capable)

**T3 · F1.1 verify-no-evals + PASS-without-probe block-capable** `[~30 LOC — re-review correction]`
- Touches: `verify_lint.py` (feature-scope branch; warn default, strict block; flip-ready flag).
- **T3b escape gap (re-review):** `verify_lint` has **NO bypass token** today → ADD a `bypass-verify`/`progress-skip` single-shot token, else R5.1 "escalate-and-HOLD" on the crisp no-evals gate would deadlock (no instant override; only strict-off escapes now).
- Acceptance: `ev1-default-noevals-blocks`. Verification: feature spec 0-eval → block (strict now); non-feature → warn; bypass token → override.
- Risk: FP on non-feature spec → feature-scope guard via `coverage_config.json`.

**T4 · F1.2 block /implement on 0-eval feature spec** `[~15 LOC]`
- Touches: `analyze_halt_gate.py` (or tasks gate).
- Acceptance: `ev1c-noevals-blocks-implement` — 0-eval feature spec → `/implement` blocked w/ remedy "run /eval-define".
- Risk: FP → only feature-scope.

## Phase 2 — ② implement-doc

**T5 · F2.1 reject 0-byte/section-missing sidecar** `[~15 LOC]`
- Touches: `implement_notes_gate.py:302`.
- Acceptance: `ev2-empty-sidecar-blocks`. Verification: 0-byte → block; missing a header → block.
- Risk: legit minimal sidecar → require only the 4 headers exist, not length.

**T6 · F2.2 orchestrator honor enforce_mode + SD-match** `[~25 LOC]`
- Touches: `implement_orchestrator.py` (remove hardcoded `verdict="warn"`; add block branch); wire `missing_sd_detector`.
- Acceptance: `ev2b-orchestrator-can-block`. Verification: 0-SD declared + N modified → block.
- Risk: snapshot absent → degrade to warn (R4).

## Phase 3 — ③ review (new hook)

**T7 · F3.1 review_proof_gate.py** `[~30 LOC + settings wire]`
- Touches: NEW `templates/claude/hooks/review_proof_gate.py` + `settings.json` Stop chain + lint/E402 config.
- Do: on `/review` count-table, parse each `Proof: path:line` → exists + tool touched it (reuse `_tool_results_by_id`/turn-scan); convergence-cap; `run_main_safe` + `stop_hook_active`.
- Acceptance: `ev3-fake-proof-blocks`. Verification: fake path:line → block; real → pass.
- Risk: FP on finding citing untouched path → warn-first + R5.3.

## Phase 4 — ④ independent (F4.1 + F4.2, riskiest)

**T8 · F4.1 deny Write subagents/** `[~30 LOC — re-review correction]`
- **T8a PREREQUISITE:** empirically confirm the HARNESS (not the agent) writes `subagents/*.jsonl` (instrument a real Task spawn); if the agent writes there, redesign.
- **T8b mechanism gap (re-review):** `invariant_guard` has **NO path-deny rule type** — it only checks `must_keep_*` patterns + skips invariants without them (`invariant_guard.py:316-318`). → ADD a `deny_write_glob` rule-type to `invariant_guard.py` FIRST; a bare invariant entry won't fire.
- Touches: `invariant_guard.py` (new deny rule-type) + `invariants.json` (`no-subagents-forge`, glob `**/.claude/projects/**/subagents/**`).
- Acceptance: `ev4-subagents-write-denied`. Verification: agent Write there → deny; `.review_verdict/` (F4.2 path) NOT denied.
- Risk: must NOT collide with F4.2's verdict path (verified outside the deny-glob).

**T9 · F4.2 reviewer-authored verdict** `[~40 LOC — BIGGEST, ship warn-capable]`
- Touches: `independent_review_gate.py` + `tools/independent_review.py` + `independent-review` skill (reviewer writes verdict via StructuredOutput to a path the main agent doesn't author; gate reads that, not `.independent_review.json`).
- Acceptance: `ev4b-forged-verdict-rejected`. Verification: hand-written verdict (no reviewer transcript) → not honored.
- Risk: contract change reviewer↔gate → ship warn-capable + **own adversarial round** (riskiest item per directive); keep invariant `independent-review-cap`.

## Phase 5 — de-risk + tests + release

**T10 · R5.2 telemetry flip-readiness** `[~15 LOC]`
- Touches: a small `flip_readiness` report reading `.hook_fire_log.json` (FP-rate per trigger) + `bypass_rate_alarm`.
- Acceptance: report shows per-trigger FP-rate; flip only when ≈0.

**T11 · Regression tests (one per ev*)** `[subprocess; UTF-8 + USERPROFILE]`
- Touches: `tests/test_*` (new). Each ev* → a subprocess test (v0.32/v0.33 lessons).
- Acceptance: all ev* green; `make rebuild` GREEN.

**T12 · R5.3 adversarial review per gate** `[process, not code]`
- Do: independent fresh-context review of EACH gate for bypass AND false-positive before any block-default flip. Score not self-claimed.

**T13 · Release** `[~10 LOC + docs]`
- Touches: `lib/installer.py` (__version__ 0.34.0), `CHANGELOG.md` ([0.34.0] + **migration note**: block-default for ALL installs on upgrade + one-line opt-out `enforce_mode default=warn` + upgrade notice), spec status→verified after /verify.
- Risk (Q1=a): upgrade-shock → the migration note + opt-out + convergence-cap (T2) are the mitigation.

---
**Convergence note:** T2 (convergence-cap) is a dependency for T3-T9 (every block gate wires it) — do T1+T2 first.
**Flip note:** v0.34 ships everything block-CAPABLE @ WARN. The block-default FLIP (per-trigger, after FP≈0 via T10/T12) is a follow-up config step, NOT in the initial v0.34 ship — so no paralysis risk at release.
