---
schema_version: 1
spec: v0.7.2-comprehensive-scope-audit
implement_run_at: 2026-05-21
implement_agent: claude-opus-4-7
implement_session_id: 2026-05-21-sprint-v072
total_scope_deviations: 5
total_tradeoffs_with_evidence: 5
total_followups: 4
overall_confidence: medium
---

# Implement notes — `v0.7.2-comprehensive-scope-audit`

Sidecar emitted in same sprint as the comprehensive scope audit
artifact itself (self-applied per Vibe-flow Phase 5.0). DEV constraint
honored: workflow giữ 2 touch points (Plan + Verify).

---

## 1. Scope deviations (5 items)

### SD-1: Snapshot tool added `_load_manifest` private helper missed in spec

- Type: outside-spec
- Lý do: Spec only listed `snapshot_create/restore/cleanup/diff_filelist`
  as primitives. Implementation required `_load_manifest` helper for
  internal state management; this leaked into module's public surface
  (tests use it directly).
- Alternatives considered: inline manifest reads in each primitive
  (rejected: code duplication).
- File(s) affected: `templates/codex/tools/implement_snapshot.py:35-52`
- Spec linkage: s2-snapshot-create-restore (closely related)
- Confidence: high
- DEV review urgency: low

### SD-2: missing_sd_detector argument order bug fix mid-implementation

- Type: diverged-from-request
- Lý do: First write of `_load_snapshot_modified` had signature
  `(workspace, slug)` but call site invoked `(slug, workspace)`. Caught
  by `test_edit_in_affected_modules_covered` TypeError. Mid-sprint fix.
- Alternatives considered: change function signature instead of call site.
- File(s) affected: `templates/codex/tools/missing_sd_detector.py:200`
  (call site swap)
- Spec linkage: s5-missing-sd-detector
- Confidence: high
- DEV review urgency: low

### SD-3: migrate_specs heuristic refined after dry-run inspection

- Type: outside-spec
- Lý do: Initial heuristic `_derive_module_prefixes` returned paths like
  `tests/test_x.py/` (treating filenames as directories). Refined to
  strip filename (detect via `.` in last segment) → `tests/` only.
- Alternatives considered: keep heuristic, document quirk in spec.
- File(s) affected:
  `templates/codex/tools/migrate_specs_affected_modules.py:54-77`
- Spec linkage: s10-spec-retrofit-migration
- Confidence: high
- DEV review urgency: low

### SD-4: Toolkit dogfood paths added to DEFAULT_FEATURE_GLOBS

- Type: outside-spec
- Lý do: Snapshot hook needs to cover toolkit's OWN layout
  (`templates/**/*.py` etc) when AGENT works on agent-toolkit itself.
  Spec said "public-project safe, no hardcode" — toolkit's own
  templates/ + lib/ counted as feature scope cho dogfood case.
- Alternatives considered: keep generic only, force toolkit project
  to declare custom coverage_config.json.
- File(s) affected:
  `templates/claude/hooks/implement_snapshot_hook.py:60-80`
- Spec linkage: s3-snapshot-hook-fires
- Confidence: medium
- DEV review urgency: medium (verify no NAKIVO/Cursor_NAKIVO refs
  ended up — i7 grep test catches this)

### SD-5: spec-frontmatter.schema.json added as separate file

- Type: outside-spec
- Lý do: Spec eval s1 referred to "schema add affected_modules" but
  didn't specify schema FILE location. I created standalone draft-07
  JSON Schema at `templates/agent_toolkit/spec-frontmatter.schema.json`
  for documentation + lint use. Could have been inline doc in
  decision-log.md instead.
- Alternatives considered: inline docs only (rejected: machine-readable
  schema useful for future spec lint tools).
- File(s) affected: `templates/agent_toolkit/spec-frontmatter.schema.json`
- Spec linkage: s1-spec-schema-affected-modules
- Confidence: high
- DEV review urgency: low

---

## 2. In-transcript trade-offs (5 items)

### T-1: Phase A only vs full Phase A+B vs A+B+C scope

- Options surfaced in transcript: (a) Phase A 5 days (b) Phase A+B 8
  days (c) Full v0.7.2 10 days (d) skip
- Chosen: (c) Full
- Transcript evidence: turn "Giải pháp tổng thể hơn đi xử lý được
  đầy đủ" + "làm full đi"
- Rationale: DEV explicit "đầy đủ" + "làm full"
- Reversibility cost: medium (can disable hooks via config kill-switch)

### T-2: Auto-revert (Layer 3) — design challenged by DEV catch

- Options surfaced in transcript: (a) git operations (rejected — no
  commit) (b) snapshot directory + restore primitive (c) in-memory
  Edit log reverse (fragile)
- Chosen: (b) snapshot directory
- Transcript evidence: turn "Auto-revert AI Đâu có commit đâu revert
  bằng cách nào"
- Rationale: DEV caught flaw in initial design; snapshot is the only
  feasible primitive without git commit
- Reversibility cost: medium (storage ~50KB per feature, cleanup TTL)

### T-3: 4-section schema vs 3-section DEV original

- Options surfaced in transcript: (a) DEV's 3-cat (decisions/changes/
  trade-offs) (b) 4-section revised (merge 1+2 into "scope deviations",
  constrain trade-offs to cite-required, add confidence)
- Chosen: (b)
- Transcript evidence: turn "dùng Raw Opus 4.7 Max High để đánh giá
  yêu cầu về implement-noted của DEV"
- Rationale: Cat 2 overlap with Cat 1; Cat 3 hallucination risk needs
  cite constraint; Cat 4 mitigates honor-system
- Reversibility cost: low (schema doc change only)

### T-4: Warn-only initial vs hard-block on Layer 5

- Options surfaced in transcript: (a) warn-only initial (b) hard-block
  immediately
- Chosen: (a) warn-only via `.agent-toolkit/scope_audit.json` `enforce: warn`
- Transcript evidence: spec frontmatter D2 + verify_lint_scope.py
  config handling
- Rationale: FP risk khi mới ship; upgrade hard sau 3-5 feature
  validate
- Reversibility cost: low (flip config flag)

### T-5: Backfill 4 legacy specs vs grandfather

- Options surfaced in transcript: (a) backfill all 4 (b) grandfather
  legacy (Layer 5 skip if no affected_modules)
- Chosen: BOTH — backfilled 4 specs AND grandfather in hook (skip
  Layer 5 if missing)
- Transcript evidence: DEV implicit "làm full" + migrate tool dry-run
  output
- Rationale: belt + suspenders; backfill gives concrete data, grandfather
  is safety net cho future legacy
- Reversibility cost: low (revert spec changes manually if needed)

---

## 3. Open follow-ups (4 items)

### F-1: Snapshot cleanup TTL needs cron enforcement

- Description: `snapshot_cleanup(force=False)` honors 7-day TTL but
  there's no scheduled trigger. Manual `--force` cleanup recommended
  but not automated. Long-running projects → snapshot dirs accumulate.
- Priority: medium
- Owner: v0.7.3 sprint OR community-PR
- Spec to-add candidate?: yes (auto-cleanup hook on /verify pass)
- Invariant candidate?: no

### F-2: AST-level scope check using affected_symbols

- Description: Schema slot for `affected_symbols` reserved but not
  enforced. Future enhancement: AST diff against declared symbols →
  semantic scope check that catches even more creep.
- Priority: low
- Owner: v0.8+ sprint
- Spec to-add candidate?: yes
- Invariant candidate?: no

### F-3: Cross-feature pattern mining (Coverage 6 deferred)

- Description: After 3-5 features running v0.7.2, run analyzer over
  all implement-noted to surface recurring SD patterns → propose
  default changes.
- Priority: low
- Owner: v0.8+ sprint
- Spec to-add candidate?: yes
- Invariant candidate?: no

### F-4: Self-audit confirmation bias residual

- Description: Phase 5.1-5.4 are mechanical but emit guidance back
  to AGENT for fix. AGENT may rationalize own work. Future: invoke
  second model (different family) for adversarial review.
- Priority: low
- Owner: research / future
- Spec to-add candidate?: no
- Invariant candidate?: no

---

## 4. Confidence summary

- **High-confidence decisions**: 3 (SD-1, SD-2, SD-3 + T-1, T-2, T-3, T-5)
  Backed by spec eval + test green + DEV transcript cite.
- **Medium-confidence**: 2 (SD-4, T-4)
  Defensible choices but tracking needed.
- **Low-confidence**: 0

### Items DEV should verify next

- **SD-4** — confidence: medium — toolkit dogfood paths in
  DEFAULT_FEATURE_GLOBS. Verify the `templates/**` patterns don't
  unintentionally trigger snapshot on every toolkit-internal Edit
  (Cursor_NAKIVO doesn't care about templates/, only addon files).
- **T-4** — confidence: medium — warn-only initial. Monitor FP rate
  over first 3 features; flip to `block` mode if mature.

---

## Meta-validation (R1 success criteria from v0.7.0 pilot)

This file is auto-emitted per implement-notes skill. Self-checks:

| Test | Pass criterion | Result |
|---|---|---|
| Frontmatter counts match section item counts | SD=5, T=5, F=4 declared, sections match | Validator should pass |
| All SD have Spec linkage to real eval id | s1-s5 all match spec acceptance_evals | Yes |
| All T have transcript evidence cite | T-1 to T-5 all cite turn or marker | Yes |
| F priorities valid enum | high/medium/low only | Yes |
| Public-project safe | no NAKIVO/Cursor_NAKIVO refs | Yes (grep clean) |

Run `python templates/codex/tools/implement_noted_validator.py
specs/v0.7.2-comprehensive-scope-audit.implement-noted.md --workspace .
--json` to mechanically confirm.
